// Injected by Vite from package.json, so the About screen cannot drift from the release.
export const APP_VERSION: string = __APP_VERSION__;
export const REPO_URL = "https://github.com/shayanline/OpenIPTV";
